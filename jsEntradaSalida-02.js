/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{
var nombre;

nombre=prompt("ingrese su nombre");
alert("usted se llama: " + nombre);

}


































  /* for(i=0;i<1;i++)
   {

   	nombreIngresado=prompt("ingrese el tipo de producto: 'ALCOHOL', 'IAC', 'QUAT'");

   	while (isNaN(nombreIngresado)==false||nombreIngresado!='ALCOHOL'&&nombreIngresado!='IAC' &&nombreIngresado!='QUAT)
   	{
   		nombreIngresado=prompt("Erro, ingrese el tipo de producto ");
   	}

   	precioIngresado= prompt("Ingrese el importe de producto:mayor a 100 y menor a 300")
   	precioIngresado=parseInt(precioIngresado);
   	while(isNaN(precioIngresado)==true&&precioIngresado<100||precioIngresado>300)
   	{
   		precioIngresado=prompt("Error, reingrese precio producto: mayor a 100 y menor a 300");
   		precioIngresado=parseInt(precioIngresado);
   	}

   	cantidadIngresada=prompt("Ingrese la cantidad de producto: hasta 1000 unidades ");
   	cantidadIngresada=parseInt(cantidadIngresada);
   	while(isNaN(cantidadIngresada)==true||cantidadIngresada>1000&&cantidadIngresada<1)
   {

   	    cantidadIngresada=prompt("Error. reingrese cantidad, hasta 1000 unidades ");
   	    cantidadIngresada=parseInt(cantidadIngresada);

   	{


   	if(acumuladorTotaldesinfectante>acumuladorTotaldesbactericida&& acumuladorTotaldesinfectante>acumuladorTotaldetergente)
   	{
   	  document.write("La categoria con mas unidades es : desinfectante" ," <br>");
   	} else
   		{
   		  if(acumuladorTotaldesbactericida>acumuladorTotaldesinfectante&&acumuladorTotaldesbactericida>acumuladorTotaldetergente)
   		 {
   	      document.write("La categoria con mas unidades es : bactericida, "<br>"");

   	    }
   	    	else
   	        {
   	           document.write("La categoria con mas unidades es : detergente, "<br>"");











































































































































*/


